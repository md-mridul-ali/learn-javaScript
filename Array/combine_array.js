// 5.Combining Arrays
    // Instructions:

    // Create two arrays of your choice.
    // Use the concat method to combine the two arrays into a new array.
    // Print both the original arrays and the combined array using console.log().

let foods=['fried rice', 'chili chicken', 'chicken fry', 'onion chicken'];
let fruits=['Apple', 'Grapes', 'Orange', 'Banana', 'Mango'];
let combineArray= foods. concat(fruits);

console.log("foods array:",foods);
console.log("fruits array:",fruits);
console.log("combine array:",combineArray);