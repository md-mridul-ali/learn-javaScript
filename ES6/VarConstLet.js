//console.log(name);
//If we declare a variable using const data type, we can not use it before initialization(It gives error), and we can not change the value or reassign the value
const name = 'Mridul';
console.log(name);
// console.log(age);
//If we declare a variable using let data type, we can not use it before initialization(It gives error), but we can change the value and reassign the value 
let age = 20;
// console.log(age);
age = 22;
console.log(age);
// console.log(location);
// If we declare a variable using var data type, and we try to use it before initialization it will not shows error it will give undefined
var location = 'Dhaka';
console.log(location);